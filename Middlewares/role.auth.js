// const verifyRole = (roleData) => {
//   return (req, res, next) => {
//     const Roles = req.body.role;

//     if (Roles.includes(roleData)) {
//       return next();
//     } else {
//       return res.json({ message: "You are not allowed" });
//     }
//   };
// };

// export default verifyRole;
